<?xml version="1.0" encoding="EUC-KR"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.webService.platform.dao.PlatformDao">
    <select id="insertAPI" resultType="map">
        SELECT
        columnTitle1,
        columnTitle2,
        columnTitle3
        FROM TEST.TABLEONE
        WHERE ID = #{ID}
    </select>

    <select id="insertAPISecond" resultType="map">
        SELECT
        columnTitle1,
        columnTitle2,
        columnTitle3
        FROM TEST.TABLETWO
        WHERE ID = #{ID}
    </select>

</mapper>
